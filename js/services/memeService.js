'use strict'

var gMeme = {
    selectedImgId: 5,
    selectedLineIdx: 0,
    lines: [{
        txt: 'I sometimes eat Falafel',
        size: 20,
        fontSize: 48,
        align: 'center',
        color: 'red'
    }]
}

function getMeme() {
    return gMeme
}